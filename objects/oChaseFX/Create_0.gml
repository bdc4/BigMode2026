// Toggle this from your police system:
enabled = false;

// Dimming + siren tuning
dim_alpha = 0.45;      // 0..1 overall darkness
flash_speed = 6.5;     // how fast it alternates
sweep_speed = 1.8;     // how fast the lights move across screen

// Light sizes (in GUI pixels)
light_r1 = 220;        // inner radius
light_r2 = 520;        // outer radius (soft edge)

// Optional: slight vignette strength
vignette_alpha = 0.12;
